# Note: opentelemetry-sdk>=1.24.0 is required for TraceIdRatioBased
# Older versions use TraceIdRatioBasedSampler (handled via compatibility import)
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-semantic-conventions>=0.40b0
aiohttp>=3.9.0
requests>=2.31.0

